services:
  web:
    build: .
    ports:
      - '3000:3000'
    # Ortam değişkenlerini .env dosyasından okur (örn. CONTACT_RECEIVER_EMAIL).
    # Deploy ederken proje kökünde bir .env dosyası oluşturun.
    env_file:
      - .env
    restart: unless-stopped
